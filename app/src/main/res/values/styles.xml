<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/dark_gray</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="HeaderBar">
        <item name="android:background">#333333</item>
    </style>

    <style name="ThemeOverlay.DarkTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:colorBackground">@color/mid_gray</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="ThemeOverlay.LightTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:colorBackground">#DDDDDD</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="date_picker_theme" parent="@android:style/Widget.DatePicker"/>
    <!--<style name="time_picker_theme" parent="@android:style/Widget.TimePicker"/>-->
    <!--<style name="time_picker_theme" parent="@android:style/Theme.Holo.Light">-->
        <!--<item name="android:timePickerStyle">@android:style/Widget.Material.TimePicker</item>-->
    <!--</style>-->
    <!--<style name="date_picker_theme" parent="@android:style/Theme.Holo.Light">-->
        <!--<item name="android:datePickerStyle">@android:style/Widget.DatePicker</item>-->
    <!--</style>-->

</resources>
